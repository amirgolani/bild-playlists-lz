<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/assets/css/style_11Themen.css">
    <title>TOUCH LAGEZENTRUM</title>
    <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
    <script src="https://cdnjs.cloudflare.com/ajax/libs/gsap/3.12.3/gsap.min.js"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css">


</head>

<body style="background-color: black; width: 1920px; height: 1080px;">
    <div style="width: 1920px; 
            height: 1080px; 
            position: absolute;
            overflow: clip;">
        <div id="backgroundImage" style="width: 1920px; 
            height: 1080px; 
            position: absolute; 
            background-image: url(/assets/gp/home6.jpg); 
            background-size: cover;">
        </div>
    </div>

    <style>
        /* Style the handle (thumb) of the range input */
        input[type="range"]::-webkit-slider-thumb {
            width: 20px;
            /* Set the width of the thumb */
            height: 20px;
            /* Set the height of the thumb */
            background-color: blue;
            /* Set the background color of the thumb */
            border: none;
            /* Remove the default border */
            border-radius: 0;
            /* Ensure square shape by removing border-radius */
            appearance: none;
            /* Remove default styles on Chrome and Safari */
        }
    </style>

    <video style="opacity: 0;" id="video" playsinline autoplay muted loop>
        <source src="/assets/gp/loop-ukr.webm" type="video/webm">
    </video>

    <div id="layoutBuildUp" style="width: 1920px; 
        height: 1080px; 
        position: absolute; 
        object-fit: fill; 
        background-position: center;
        ">
    </div>

    <div id="mainTitle" style="left: 320px; 
        top: -500px; 
        position: absolute">
        <span style="color: white; 
            font-style: italic; 
            font-size: 138px; 
            font-family: Gotham Condensed; 
            font-weight: 600; 
            line-height: 96px; 
            word-wrap: break-word;">RUSSLANDS<br /></span>
        <span style="color: white; 
            font-style: italic; 
            font-size: 228px; 
            font-family: Gotham Condensed; 
            font-weight: 900; 
            line-height: 206px; 
            word-wrap: break-word">KRIEG</span>
    </div>

    <div id="seek" hidden>

        <a class="floatLeft0" onclick="ctrlPlayVideo()">
            <i class="material-icons align">play_arrow</i>
        </a>
        <a class="floatLeft" onclick="pauseVideo()">
            <i class="material-icons align">pause</i>
        </a>

        <div style="font-size: 26px; padding-left: 20px;
        font-family: Gotham; 
        font-weight: 400; 
        line-height: 20px;" id="timecode">00:00</div>

        <input id="seekslider" type="range" min="0" max="1500" value="0" step="1" style="
        position: absolute;
        bottom: 125px; left: 200px;
        width: 400px; /* Set the width of the range input */
        height: 10px; /* Set the height of the range input */
        background-color: white; /* Set the background color of the track */
        border: none; /* Remove the default border */
        appearance: none; /* Remove default styles on Firefox */
        outline: none; /* Remove the default focus outline */
      ">

    </div>

    <script>
        // Highlightet Button *//
        function highlight(e) {
            // var videos = document.getElementsByClassName("video");
            // for (const video of videos) {
            //     video.classList.remove("active");
            // }
            // e = e || window.event;
            // e.srcElement.classList.add("active");
            // console.log(e.srcElement);
        }

        // Unhighlightet Button *//
        function unhighlight(e) {
            var videos = document.getElementsByClassName("video");
            for (const video of videos) {
                video.classList.remove("active");
            }
        }

        // Audio optional part *//
        function playVideo(path, loop, volume = "muted", PlayButtons = "withPlayButtons") {
            var video = document.getElementById("video");
            var seek = document.getElementById("seek");

            if (PlayButtons == "withPlayButtons") {
                seek.hidden = false;
                console.log("withPlay");
            } else {
                seek.hidden = true;
            }


            console.log(video);
            video.setAttribute('src', path)

            if (loop == "loop" || loop == "true")
                video.loop = true;
            else {
                video.loop = false;
            }

            if (volume == "muted") {
                video.muted = true;
                video.volume = 0;
                console.log("Sem Audio");
            } else {
                video.muted = false;
                video.volume = 1;
                console.log("Com Audio");
            }
        }


    </script>

    <script name="Seek">
        var seekslider;
        var video = document.getElementById('video'), frameTime = 1 / 25;
        seekslider = document.getElementById("seekslider");
        // Add event listeners
        seekslider.addEventListener("change", vidSeek, false);
        video.addEventListener("timeupdate", seektimeupdate, false);
        video.ontimeupdate = function () { timecodeUpdate() };
        function pauseVideo() {
            video.pause();
            console.log("pause");
        }
        function ctrlPlayVideo() {
            video.play();
            console.log("play");
        }
        function framePlus() {
            video.currentTime = Math.min(video.duration, video.currentTime + frameTime);
        }
        function frameMinus() {
            video.currentTime = Math.max(0, video.currentTime - frameTime);
        }

        function vidSeek() {
            pauseVideo();
            var seekto = video.duration * (seekslider.value / 1500);
            video.currentTime = seekto;
        }
        function seektimeupdate() {
            var nt = video.currentTime * (1500 / video.duration);
            seekslider.value = nt;
        }
        function timecodeUpdate() {
            // Display the current position of the video in a <p> element with id="demo"
            document.getElementById("timecode").innerHTML = formatSecondsAsTime(video.currentTime);
        }
        function formatSecondsAsTime(secs, format) {
            var hr = Math.floor(secs / 3600);
            var min = Math.floor((secs - (hr * 3600)) / 60);
            var sec = Math.floor(secs - (hr * 3600) - (min * 60));
            if (min < 10) {
                min = "0" + min;
            }
            if (sec < 10) {
                sec = "0" + sec;
            }

            return min + ':' + sec;


        }
    </script>

    <script name="getAndMakeLayout">
        function getLayout() {

            var layout = `<div id="theMenu" style="width: 220px; 
                    height: 1080px; 
                    padding-left: 50px; 
                    left: -500px; 
                    top: 0px; 
                    position: absolute; 
                    background: linear-gradient(90deg, rgba(0, 0, 0, 0.5) 0%, rgba(0, 0, 0, 0) 100%); 
                    flex-direction: column; 
                    justify-content: flex; 
                    align-items: center; 
                    gap: 12px; 
                    display: inline-flex">
                    <div onclick="handleSelect(this.id); pauseVideo(); unhighlight();" id="b_0" 
                    style="align-self: stretch; 
                        height: 100px; 
                        padding: 10px; 
                        margin-top: 50px; 
                        background-image: linear-gradient(180deg, rgba(0, 0, 0, 0) 0%, rgb(0,0,0,1) 100%), url(/assets/gp/home6.jpg);
                        background-size: cover;
                        background-position: center;
                        border-radius: 10px; 
                        border: 1px #e5e5e5 solid; 
                        border-left: 4px #e5e5e5 solid;
                        border-right: 4px #e5e5e5 solid;
                        justify-content: center; 
                        align-items: center; 
                        gap: 10px; 
                        display: inline-flex">
                        <div style="text-align: center; 
                            color: white; 
                            font-size: 20px; 
                            font-family: Gotham; 
                            font-weight: 400; 
                            line-height: 20px; 
                            word-wrap: break-word">
                            <i class="fa-solid fa-house"></i>
                            </div>
                    </div>
                    `;



            fetch('/layout-ukr').then((response) => response.json()).then((json) => {

                for (l = 1; l < json.length; l++) {
                    var filenameArr = json[l].file.split('storage-ukr')[1].substring(1);
                    console.log(`${filenameArr}`)
                    layout += ` 
                    <div onclick="handleSelect(this.id); playVideo('/assets/storage-ukr/${filenameArr}', '${json[l].loop ? 'loop' : 'notloop'}', '${json[l].mute ? 'muted' : 'unmuted'}', '${json[l].ctrl ? 'withPlayButtons' : 'noPlayButtons'}'); highlight();" id="b_${l}" 
                    style="align-self: stretch; 
                        height: 26px; 
                        padding: 10px; 
                        background-image: linear-gradient(180deg, rgba(0, 0, 0, 0) 0%, black 125%), url(/assets/storage-ukr/${filenameArr.split('.')[0]}.jpg);
                        background-size: cover;
                        background-position: center;
                        border-radius: 10px; 
                        border: 1px #e5e5e5 solid; 
                        border-left: 4px #e5e5e5 solid;
                        border-right: 4px #e5e5e5 solid;
                        justify-content: center; 
                        align-items: center; 
                        gap: 10px; 
                        
                        display: inline-flex">
                        <div style="text-align: center; 
                            color: white; 
                            font-size: 20px; 
                            font-family: Gotham; 
                            font-weight: 500; 
                            line-height: 20px; 
                            text-transform: uppercase;
                            word-wrap: break-word">
                            ${json[l].name}
                            </div>
                    </div>`

                }

                document.getElementById('layoutBuildUp').innerHTML = layout

            });

        }

        getLayout()
    </script>

    <script name="handleSelect">

        var selectedElement = "b_0"

        function handleSelect(newID) {
            var seek = document.getElementById("seek");

            if (newID === "b_0") {
                seek.hidden = true
                gsap.to("#mainTitle",
                    {
                        duration: 1, // animation duration in seconds
                        top: 60, // target height
                        ease: "power2.inOut", // easing function
                    });
                gsap.to("#video",
                    {
                        duration: 1, // animation duration in seconds
                        opacity: 0, // target height
                        ease: "power2.inOut", // easing function
                    });
                gsap.fromTo("#backgroundImage",
                    { scale: 1.5 },
                    {
                        duration: 1, // animation duration in seconds
                        scale: 1, // target height
                        ease: "power2.inOut", // easing function
                    });
            } else {
                gsap.to("#mainTitle",
                    {
                        duration: 1, // animation duration in seconds
                        top: -500, // target height
                        ease: "power2.inOut", // easing function
                    });
                gsap.to("#video",
                    {
                        duration: 1, // animation duration in seconds
                        opacity: 1, // target height
                        ease: "power2.inOut", // easing function
                    });
                gsap.fromTo("#backgroundImage",
                    { scale: 1 },
                    {
                        duration: 0, // animation duration in seconds
                        scale: 0, // target height
                        ease: "power2.inOut", // easing function
                    });
                gsap.fromTo("#video",
                    { opacity: 0 },
                    {
                        duration: 1, // animation duration in seconds
                        opacity: 1, // target height
                        ease: "power2.inOut", // easing function
                    });
            }

            gsap.to(`#${newID}`,
                {
                    duration: 1, // animation duration in seconds
                    height: 100, // target height
                    ease: "power4.inOut", // easing function
                });



            gsap.to(`#${selectedElement}`,
                {
                    duration: 1, // animation duration in seconds
                    height: 26, // target height
                    ease: "power4.inOut", // easing function
                });

            selectedElement = newID;

        }

        // GSAP animation

        setTimeout(() => {
            gsap.fromTo("#theMenu",
                {
                    left: -500, // target height
                },
                {
                    left: 0, // target height,
                    duration: 1,
                    delay: .2,
                    ease: "power1.inOut", // easing function

                });

            gsap.fromTo("#mainTitle",
                {
                    top: -500
                }, // target height,
                {
                    top: 60, // target height,
                    duration: 1.2,
                    ease: "power1.inOut", // easing function

                });
        }, 100);


    </script>




</body>

</html>