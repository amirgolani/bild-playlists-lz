<!DOCTYPE html>
<html lang="en">

<head>
    <title>TOUCH LAGEZENTRUM</title>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <!-- <link rel="stylesheet" href="/assets/css/style_11Themen.css"> -->
    <link rel="stylesheet" href="/assets/css/gp_styles.css">
    <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
    <script src="https://cdnjs.cloudflare.com/ajax/libs/gsap/3.12.3/gsap.min.js"></script>
    <script src="https://unpkg.com/gsap@3.9.0/dist/Draggable.min.js"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css"
        integrity="sha512-DTOQO9RWCH3ppGqcWaEA1BIZOC6xxalwEsw9c2QQeAIftl+Vegovlnee1c9QX4TctnWMn13TZye+giMm8e2LwA=="
        crossorigin="anonymous" referrerpolicy="no-referrer" />
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/brands.min.css"
        integrity="sha512-8RxmFOVaKQe/xtg6lbscU9DU0IRhURWEuiI0tXevv+lXbAHfkpamD4VKFQRto9WgfOJDwOZ74c/s9Yesv3VvIQ=="
        crossorigin="anonymous" referrerpolicy="no-referrer" />
    <style>
        body {
            display: flex;
            align-items: center;
            justify-content: center;
            height: 100vh;
            margin: 0;
            background-color: rgb(17, 17, 17);
        }
    </style>
</head>

<body>

    <body>

        <div id="layouts-list"></div><br>
        <div
            style="width: 300px; height: 100px; background-color: black; border-radius: 10px; border: 3px solid #232323; padding: 10px;">
            <div style="font-size: 18px; font-family: Gotham; font-weight: 700; line-height: 20px; opacity: 1; color: white"> Name</div>
        </div>
        <script>
            // Function to fetch data from the '/layouts' endpoint
            function fetchData() {
                fetch('/layouts')
                    .then(response => response.json())
                    .then(data => {
                        console.log(data);

                        document.getElementById('layouts-list').innerHTML = data

                    })
                    .catch(error => console.error('Error fetching data:', error));
            }

            // Function to display layouts in a stylized list
            function displayLayouts(layouts) {

            }

            // Fetch data when the page loads
            fetchData();

            function stylizeArray(array) {
                let result = '<ul>';

                array.forEach(item => {
                    result += '<li>';

                    if (item.isDirectory) {
                        result += `<strong>${item.name}</strong>`;
                        result += stylizeArray(item.layout);
                    } else {
                        result += `<span>${item.title[0]}</span>`;
                        result += `<span>${item.type[0]}</span>`;
                        result += `<span>${item.file}</span>`;
                        // Add more properties as needed

                        // Add thumbnail if available
                        if (item.thumb) {
                            result += `<img src="${item.thumb}" alt="${item.title[0]} Thumbnail" />`;
                        }
                    }

                    result += '</li>';
                });

                result += '</ul>';
                return result;
            }
        </script>



    </body>

</html>